variable "inventory_functions_invoke_arns" {
  description = "A map of function names to their invoke ARNs."
  type        = map(string)
  
}
variable "inventory_functions_names" {
  description = "A map of function names to their invoke ARNs."
  type        = map(string)
  
}